// Previous > Arguments

function square(num) { // num is parameter, not an argument; parameter is place holder name
    console.log(num *num);
}

// argument is this:
square(4)


function sum(x, y) {
    console.log(x + y);
}

function divide(a, b) {
    console.log(a / b);
}


// Next > The Return Statement